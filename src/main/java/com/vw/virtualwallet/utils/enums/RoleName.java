package com.vw.virtualwallet.utils.enums;

/**
 * Enum representing different roles in the Virtual Wallet application.
 * This enum is used to define user roles for access control and permissions.
 *
 * @author caito
 *
 */
public enum RoleName {
    ROLE_ADMIN,
    ROLE_USER,
    ROLE_SUPERVISOR
}
